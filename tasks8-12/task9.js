const age = document.querySelector('th[data-type="number"]');
const name = document.querySelector('th[data-type="string"]');
console.log(age)
console.log(name)

age.addEventListener('click', () => {
    const tableRows = document.querySelectorAll('tr');
})